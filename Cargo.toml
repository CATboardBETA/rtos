[workspace.package]
edition = "2024"
version = "0.1.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 3
lto = "fat"
codegen-units = 1


[workspace]
resolver = "3"
members = ["runner", "kernel"]
default-members = ["runner"]
